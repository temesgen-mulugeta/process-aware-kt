# Process-Aware KT prototype — compatible dependency ranges.
# Exact experiment versions: results/revisions/environment.txt.
# A fresh venv is recommended:  python -m venv .venv && source .venv/bin/activate

torch>=2.2,<3.0
google-genai>=1.68,<3.0        # Gemini SDK; genai.Client() auto-reads GEMINI_API_KEY
sentence-transformers>=3.0      # frozen summary embedder (MiniLM/E5)
pandas>=2.2,<3.0
numpy>=1.26,<2.0
scikit-learn>=1.4
tqdm>=4.66
python-dotenv>=1.0              # loads .env (GEMINI_API_KEY, GEMINI_MODEL)
pydantic>=2.7                   # JSON-schema-constrained analyzer output

# --- test (dev) ---
pytest>=7.4                     # run: pytest   (deterministic, no live API needed)

# ---------------------------------------------------------------------------
# FUTURE / NOT USED IN THIS PROTOTYPE (see FINDINGS.md "How to extend this"):
#   pykt-toolkit>=0.0.39         # stronger KT backbones (AKT, GKT, SAKT, DKVMN)
# ---------------------------------------------------------------------------

scipy>=1.11
matplotlib>=3.8
